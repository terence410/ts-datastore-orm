{
  "name": "ts-datastore-orm",
  "version": "2.0.1",
  "description": "ts-datastore-orm targets to provide a strong typed and structural Orm feature for Datastore (Firestore in Datastore mode).",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "test": "echo 1",
    "test:all": "nyc --reporter=json --reporter=text mocha --exit --timeout 600000 -r ts-node/register tests/**/*.ts",
    "test:general": "mocha --exit --timeout 30000 -r ts-node/register tests/general.test.ts",
    "test:query": "mocha --exit --timeout 30000 -r ts-node/register tests/query.test.ts",
    "test:performance": "mocha --exit --timeout 30000 -r ts-node/register tests/performance.test.ts",
    "test:compositeIndex": "mocha --exit --timeout 30000 -r ts-node/register tests/compositeIndex.test.ts",
    "test:subclass": "mocha --exit --timeout 30000 -r ts-node/register tests/subclass.test.ts",
    "test:errors": "mocha --exit --timeout 30000 -r ts-node/register tests/errors.test.ts",
    "test:admin": "mocha --exit --timeout 30000 -r ts-node/register tests/admin.test.ts",
    "test:transactionManager": "mocha --exit --timeout 30000 -r ts-node/register tests/transactionManager.test.ts",
    "test:lockManager": "mocha --exit --timeout 30000 -r ts-node/register tests/lockManager.test.ts",
    "test:helper:incrementHelper": "mocha --exit --timeout 30000 -r ts-node/register tests/incrementHelper.test.ts",
    "test:helper:indexResaveHelper": "mocha --exit --timeout 30000 -r ts-node/register tests/indexResaveHelper.test.ts",
    "performance:tsdatastoreorm": "tsc & node ./build/performance/tsdatastoreorm.performance.js",
    "performance:datastore": "tsc & node ./build/performance/datastore.performance.js",
    "deploy:patch": "npm version patch",
    "deploy:build": "tsc"
  },
  "dependencies": {},
  "devDependencies": {
    "@google-cloud/datastore": "^6.3.0",
    "@types/chai": "^4.2.5",
    "@types/mocha": "^5.2.7",
    "@types/node": "^12.12.14",
    "chai": "^4.2.0",
    "mocha": "^6.2.2",
    "nyc": "^15.0.0",
    "ts-node": "^8.10.1",
    "typescript": "^3.9.2"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/terence410/ts-datastore-orm.git"
  },
  "author": "Terence",
  "keywords": [
    "datastore",
    "google datastore",
    "google firestore",
    "datastore orm",
    "transaction",
    "google cloud",
    "relational db",
    "typescript"
  ],
  "license": "MIT License"
}
